- What is the command to display your print working directory?
• pwd - this command will display the print or present working directory (pwd means print working directory).


- What is the command to list all the files and directories including hidden files?
• ls -a - this command will display files, directories as well as hidden files and directories in the current working directory (ls means list, -a means include hidden files also).


- What is the command to create nested directories it means create directories inside a directory.
• mkdir -p A/B/C/D/E - this command will create a directory as well as create directories inside a directory. (mkdir means make directory, -p means parent directory).
